version: '3.8'

services:
  postgres_nlw:
    container_name: postgres_movie_ranker
    image: postgres
    ports:
      - 5434:5432
    environment:
      - POSTGRES_USER=admin
      - POSTGRES_PASSWORD=admin
      - POSTGRES_DB=pg_movie_ranker